# para verificar sintaxis:
# sudo nginx -t 
# para "recompilar":
# sudo sudo systemctl reload nginx

server {
	listen 80;
	server_name contadordeusuarios.com;
	auth_basic "admin area";
	auth_basic_user_file /etc/apache2/.htpasswd;
	location = /contador/value{
		proxy_pass http://localhost:8537/imprimir;
	}

	location /{
		return 404 '{"error": {"status_code": 404,"status": "Unauthorized"}}';
	}
}
